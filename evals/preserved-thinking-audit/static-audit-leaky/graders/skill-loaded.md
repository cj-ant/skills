---
type: tool_used
tool: Read
input_match: 'preserved-thinking-audit/references/checks\.md'
min: 1
arm: with-only
---

The slash command loaded the skill and the agent read its check catalog. A slash command expands the skill into the prompt without a `Skill` tool call, so this checks the read instead. An indicator, not part of the score when a without-skill arm runs.
